hosts: ['blade-prod-a']
args:
    username:
      value: Ralph
      help: is the user you want to use
exec:
  - echo 'Hello {{username}} who is years young'
  - cat /root/sample.json
overrides:
  concurrency: 5
